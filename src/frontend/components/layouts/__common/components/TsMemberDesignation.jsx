

const TsMemberDesignation = ({children, className=''}) => {
    return (
        <h4 className={`tsteam-member__designation ${className} `} > {children} </h4>
    )
}

export default TsMemberDesignation